{
  "name": "sports-intelligence-api",
  "version": "0.1.0",
  "private": true,
  "description": "Small typed reference API for deterministic sports query routing and feed-quality checks.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "check": "npm run test && npm run build"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.2",
    "hono": "^4.10.6",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.1.8"
  }
}
